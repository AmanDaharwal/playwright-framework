/**
 * Module to logout from EB portal
 * @param {*} page
 * @param {String{CUSTOMER/EMPLOYER/PRODUCER}} portal
 */
const logout = async (page) => {
  
  //call logout methods
};

module.exports = logout;
